# 开发与部署说明

本文档同时说明本地开发调试和 Docker 部署方式。建议先使用本地方式联调，确认前后端接口正常后，再构建镜像部署。

## 一、本地开发调试

### 1. 启动后端

后端是 Go Gin 服务，入口位于 `server/cmd/server`。首次启动前准备配置文件：

```powershell
cd D:\WorkSpace\HIS_emergency\emergency-his
Copy-Item server/config/config.example.yaml server/config/config.yaml
```

根据实际环境填写 `server/config/config.yaml` 中的 MySQL、JWT 和 HIS 接口配置。该文件包含敏感信息，不要提交到 Git。

启动后端：

```powershell
cd D:\WorkSpace\HIS_emergency\emergency-his\server
go run ./cmd/server
```

服务默认监听配置文件中的端口，前端开发代理默认请求 `http://127.0.0.1:8080`。可用健康检查确认服务是否启动：

```powershell
Invoke-WebRequest http://127.0.0.1:8080/health -UseBasicParsing
```

停止后端：在运行 `go run` 的终端按 `Ctrl + C`。

### 2. 启动前端

前端位于 `web` 目录，使用 Vue 3、Vite 和 Element Plus：

```powershell
cd D:\WorkSpace\HIS_emergency\emergency-his\web
npm install
npm run dev
```

`npm install` 只需首次执行。开发页面固定使用 `http://127.0.0.1:5173/`；端口被占用时会直接报错，不会自动切换端口。前端 `/api` 请求由 Vite 代理到后端 `http://127.0.0.1:8080`。

停止前端：在运行 `npm run dev` 的终端按 `Ctrl + C`。

### 3. 联调检查

浏览器访问：

```text
http://127.0.0.1:5173/
```

患者查询页面会请求 `/api/v1/patients`，Vite 会将其代理到后端的 `http://127.0.0.1:8080/api/v1/patients`。

如果页面显示 502，通常表示后端未启动或端口配置不一致；如果显示 500，检查 MySQL 连接和服务配置。浏览器开发者工具的 Network 面板可以查看请求地址、状态码和响应内容。

## 二、Docker 部署

### 1. 构建后端服务镜像

```bash
docker build -t emergency-his-server:0.1.0 ./server
```

### 2. 导出镜像

```bash
docker save emergency-his-server:0.1.0 -o emergency-his-server-0.1.0.tar
```

### 3. 导入镜像

```bash
docker load -i emergency-his-server-0.1.0.tar
```

### 4. 部署目录结构

```text
/opt/emergency-his/
├── deploy/
│   └── docker-compose.yml
└── server/
    └── config/
        └── config.yaml
```

### 5. 启动、查看和停止服务

```bash
cd /opt/emergency-his
sudo docker compose -f deploy/docker-compose.yml up -d
sudo docker compose -f deploy/docker-compose.yml ps
sudo docker compose -f deploy/docker-compose.yml logs -f server
```

停止 Docker 服务：

```bash
sudo docker compose -f deploy/docker-compose.yml down
```

`down` 会停止并移除 Compose 创建的容器，不会删除镜像和数据库数据卷。重新启动时再次执行 `up -d` 即可。

## 三、前端生产构建

```powershell
cd D:\WorkSpace\HIS_emergency\emergency-his\web
npm run build
```

构建结果位于 `web/dist`。生产环境应由 Nginx 提供静态文件，并将 `/api/` 转发到 Go 服务；`npm run dev` 只用于开发调试。
